# ========================================
# PLATINUM TIER - CLOUD AGENT (.env.cloud)
# ========================================
# Deploy: Oracle Cloud VM (Ubuntu 22.04)
# Role: 24/7 drafts (email/social), Git push
# Security: NO send credentials (SMTP/WhatsApp/Social tokens)

# Inherit from main .env.example + Cloud-specific overrides
TIER=platinum
ENABLE_CLOUD_AGENT=true
VAULT_PATH=/opt/personal-ai-employee/vault

# Git Sync (Cloud pushes every 60s)
GIT_REMOTE_URL=git@github.com:user/vault.git
GIT_BRANCH=main
GIT_SYNC_INTERVAL=60

# Claude API (draft generation only)
CLAUDE_API_KEY=sk-ant-xxx
CLAUDE_MODEL=claude-sonnet-4-5-20250929

# Gmail monitoring (read-only)
GMAIL_CREDENTIALS_PATH=/opt/personal-ai-employee/credentials.json
GMAIL_POLL_INTERVAL=120

# WhatsApp notifications (send alert, not messages)
WHATSAPP_NOTIFICATION_NUMBER=+1234567890
FALLBACK_EMAIL=user@example.com

# ❌ Cloud MUST NOT have (Local Agent only):
# - SMTP_PASSWORD
# - WHATSAPP_SESSION_PATH
# - LinkedIn/Social tokens
# - ODOO_PASSWORD
